name: Pipeline trigger

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/setup-node@v3

    - name: Install dependencies
      run: npm install

    - name: Run a sample script
      run: echo "This workflow must works!"
